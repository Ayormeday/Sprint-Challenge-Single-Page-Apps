import React from "react";
import { Card, Icon, Image } from 'semantic-ui-react'

export default function CharacterCard({ character }) {
  const { name, age, species, status, location, origin}
  return (
    
  );
}
